[example-site]
type = http
host = 127.0.0.1
port = 8080
inport = 80
keys = example-site.dat

# The production destination key file must remain private and must never be
# committed to a public repository. This example intentionally uses a
# placeholder filename and contains no live I2P destination.
