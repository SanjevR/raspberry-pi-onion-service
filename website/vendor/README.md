# Vendor Assets

MapLibre GL JS and PMTiles browser assets are not bundled in this sanitized package.

Run:

```bash
./scripts/fetch-map-assets.sh website
```

Then review upstream licenses and version checksums before production deployment.
