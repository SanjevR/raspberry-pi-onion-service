# Local Map Glyphs

The production site uses locally hosted PBF glyphs such as `Open Sans Regular/{range}.pbf`.

Generated/downloaded glyph directories are intentionally excluded from Git because they are large and third-party licensed. Review the OpenMapTiles fonts project and its licenses before redistribution.
